<h2>Edit</h2>
<form method="POST" action="/pokemons/{{pokemon.id}}">
 <div class="form-group">
    <label for="name">Name</label>
    <input type="text" class="form-control" name="name" value="{{pokemon.name}}" />
</div>
<div class="form-group">
    <label for="type">Type</label>
    <input type="text" class="form-control" name="type" value="{{pokemon.type}}" />
</div>
<div class="form-group">
    <label for="type">Combatpoint</label>
    <input type="integer" class="form-control" name="combatpoint" value="{{pokemon.combatpoint}}" />
</div>
</form>